# Platform Route Group

Place authenticated and operational routes here (admin panel, content workflows, and role-based tools).

Routes in this group are expected to become dynamic and protected as authentication/RBAC is introduced.

# Marketing Route Group

Place public, crawlable routes here (landing pages, service pages, article index, and article details).

This route group should default to Server Components and static-friendly rendering unless a specific
feature requires fresher data.

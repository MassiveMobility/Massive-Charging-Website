# Features

Domain-focused modules (articles, navigation, admin, auth, SEO helpers) should live here.

- `articles/`: article domain logic, types, actions, and queries.
- `navigation/`: navigation contracts and route-aware helpers.
- `seo/`: structured metadata and discoverability-focused domain logic.

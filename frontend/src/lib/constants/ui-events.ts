/**
 * Shared browser event names used to coordinate visual shell behavior
 * between independent client components (for example, hero sidebar + header).
 */
export const FRONTPAGE_SIDEBAR_VISIBILITY_EVENT = "frontpage-sidebar-visibility";
